# Team City agent - Windows

This command creates a Team City Agent on Windows Docker container.

The file buildAgent.Properties is a sample file generated by TC Server after the agent has been authorized